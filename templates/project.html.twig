{% extends 'partials/base.html.twig' %}
{% block content %}
  <div class="content-wrapper">
  {{ page.content }}
  </div>
  <div class="grid-wrapper">
    <ul class="grid-container">
    {% for image in page.media.images %}
      {% if loop.index % 4 == 0 %}
        <li class="item item--spacer"></li>
      {% endif %}
      <li class="item {% if image.metadata.gridSize %}item--{{ image.metadata.gridSize }}{% endif %}">
        {{ image.html }}
        {% if image.metadata.title or image.metadata.description %}
        <div class="item__info">
          <h2>{{ image.metadata.title }}</h2>
          <p>{{ image.metadata.description }}</p>
        </div>
        {% endif %}
      </li>
    {% endfor %}
    </ul>
  </div>
{% endblock %}